conky.config = {
    alignment = 'top_middle',
    background = true,
    border_width = 0,
    default_color = '#00f0ff',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = false,
    gap_x = 0,
    gap_y = 50,
    minimum_height = 150,
    minimum_width = 440,
    maximum_width = 440,
    no_buffers = true,
    own_window = true,
    own_window_class = 'ConkyClockWidget',
    own_window_type = 'desktop',
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_argb_value = 255,
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    update_interval = 1.0,
    use_xft = true,
}

conky.text = [[
${alignc}${color #00aaff}${font FreeMono:size=22:bold}┌───────────────────────┐${font}
${voffset 2}${alignc}${color #00f0ff}${font DS-Digital:size=52:bold}${time %H:%M:%S}${font}
${voffset 6}${alignc}${color #4db8ff}${font FreeMono:size=14:bold}${time %A, %d %B %Y}${font}
${voffset -2}${alignc}${color #00aaff}${font FreeMono:size=22:bold}└───────────────────────┘${font}
]]

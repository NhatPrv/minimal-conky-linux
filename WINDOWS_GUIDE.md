# Hướng dẫn tham khảo cho Windows

Theme trong repo này dùng Conky, nên chỉ chạy trên Linux.  
Nếu bạn dùng Windows, có thể làm giao diện tương tự bằng **Rainmeter**.

## 1) Cài công cụ

- Cài [Rainmeter](https://www.rainmeter.net/)
- Cài font monospace bạn thích (ví dụ: JetBrains Mono, Fira Code, Cascadia Code)

## 2) Chọn skin nền tảng

Bạn có thể bắt đầu từ các skin nhẹ, dễ chỉnh:

- Simple / Minimal system monitor skins
- Clock + CPU/RAM + Disk + Network skins

Mẹo: chọn skin có file `.ini` rõ ràng để sửa vị trí, màu, font nhanh hơn.

## 3) Map tính năng tương đương với theme Linux này

- **Clock widget** → skin đồng hồ số
- **CPU/RAM widget** → skin monitor tài nguyên
- **Graphics/Disk widget** → skin hiển thị GPU/Disk/Network
- **Audio status** → skin âm lượng + thiết bị input/output

## 4) Tinh chỉnh giao diện cho giống ảnh preview

- Dùng nền trong suốt
- Font đậm, cỡ vừa
- Màu nhấn xanh dương/cyan
- Căn widget theo cụm ở góc màn hình
- Giữ khoảng cách dòng đều nhau

## 5) Lưu ý quan trọng

- Dữ liệu cảm biến (GPU, nhiệt độ, ổ đĩa) trên Windows có thể cần plugin hoặc skin hỗ trợ riêng.
- Một số skin cần quyền admin để đọc đầy đủ thông số hệ thống.
- Mức “giống hệt” Conky sẽ phụ thuộc skin bạn chọn và mức độ chỉnh tay.

---

Nếu bạn muốn, mình có thể viết tiếp một bản cấu hình Rainmeter mẫu theo đúng layout của ảnh preview trong repo này.
